Teste de commit para realização da ListEx 0'
	
Fazendo merge

Editando novamente...

Exemplo de conflito:

import random import randrange

foo = ['a', 'b', 'c', 'd', 'e']
random_index = randrange(0,len(foo))
print foo[random_index]
